numpy>=1.24.0,<2  # ROS cv_bridge(apt) 는 numpy 1.x 로 빌드됨 - 2.x 면 변환 시 segfault
rerun-sdk>=0.15.0
opencv-python>=4.8.0
Pillow>=9.5.0
# 시스템 패키지:
#   sudo apt install ros-humble-cv-bridge
#   sudo apt install kazam   # 녹화 (또는 obs-studio)
#   sudo apt install shotcut # 편집 (또는 openshot-qt)
