numpy>=1.24.0,<2  # ROS cv_bridge(apt) 는 numpy 1.x 로 빌드됨 - 2.x 면 변환 시 segfault
opencv-python>=4.8.0
Pillow>=9.5.0
matplotlib>=3.7.0
# ROS2 cv_bridge 는 시스템 패키지로 설치:
#   sudo apt install ros-humble-cv-bridge
