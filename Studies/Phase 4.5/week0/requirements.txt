# sim 전용 venv (.venv-sim) 의 의존성.
# OpenVLA 공용 venv (.venv-vla) 와 분리한다 -- transformers==4.40.1 / timm==0.9.16 고정
# 조합을 건드리면 Block 1-3 실측의 재현성이 깨진다 (README "시작하기 전에").
#
# 버전을 고정하지 않는 이유: ManiSkill 은 sapien 과 함께 python 버전별 휠을 제공하고,
# 설치 가능한 조합이 환경에 따라 갈린다. 설치 후 `pip freeze > outputs/pip_freeze.txt` 로
# 실제 해석된 버전을 기록하고, 그 값을 outputs/env_build.md 에 옮긴다.
# torch 는 mani_skill 이 끌어오므로 여기 적지 않는다 (CUDA 빌드가 뒤바뀌는 것을 막기 위함).
mani_skill
gymnasium>=0.29.0
numpy>=1.24.0
Pillow>=9.5.0
