# Week 3 — RunPod LoRA 파인튜닝 (Section 0 후반 포함)


> **이번 주 목표**: 학습 환경을 컨테이너로 만들어 RunPod 에 올리고, 짧은 probe 로 1사이클 비용을 실측한 뒤 본 LoRA 사이클을 완주해 체크포인트를 확보한다.
> **예상 시간**: 10-12시간
> **핵심 질문**: "이 학습이 예산 안에서 끝나는가? 그리고 중간에 인스턴스가 회수되면 이어서 돌 수 있는가?"
> **선행**: [week2](../week2/README.md) 완료 — RLDS 데이터셋, 등록 패치, 로드 검증 로그
> **대응 Roadmap**: [`Roadmap/Phase 4.5.md`](../../../Roadmap/Phase%204.5.md) Section 0 후반 (Docker 컨테이너화 + RunPod 이관 + LoRA 1사이클 실측) + Section 2 주차 3


---


## 학습 순서


| 순서 | 단계 | 파일/자료 | 설명 |
|:----:|------|----------|------|
| 1 | 학습 측 이미지 빌드 | `PRACTICE.md` 1 | week2 환경 명세 + 등록 패치를 이미지로 고정 |
| 2 | RunPod 기동·이관 | `PRACTICE.md` 2 | pod 생성, 전송, network volume 연결 |
| 3 | probe 실측 + 게이트 판정 | `PRACTICE.md` 3 | 짧게 돌려 시간·비용·VRAM 을 재고 1사이클을 추정 |
| 4 | 본 LoRA 1사이클 | `PRACTICE.md` 4 | 체크포인트 주기 저장 + loss 추이 |
| 5 | 중단 복구 리허설 | `PRACTICE.md` 5 | 회수 대비 — 체크포인트에서 재개 |
| 6 | 퀴즈 | quiz_easy / quiz_medium | 메모리 산수 / 체크포인트 / 통계 |


---


## Section 0 게이트 — 이 주차가 통과시켜야 하는 것


Roadmap Section 0 의 남은 항목이 이번 주에 닫힌다. **게이트가 실패하면 Section 1-3 의 설계를 축소해야 하므로** 판정을 미루지 않는다.


| Section 0 항목 | 이번 주 대응 | 실패 시 |
|---|---|---|
| sim + eval 환경 Docker 컨테이너화 | 실습 1 (학습 측). eval 측은 로컬 유지 여부에 따라 범위가 갈린다 (아래) | 재현 경로 없이 진행 — 로컬 장애 시 복구 불가를 감수 |
| RunPod 에서 기동 + 재현 확인 | 실습 2 | 학습 자체가 막힌다 |
| OpenVLA 7B LoRA 1사이클이 RTX 4090 에서 가능한지 실측 | 실습 3 (probe 로 추정) + 실습 4 (완주로 확정) | 롤백 옵션 B — 경량 adaptation 으로 축소 |


> 컨테이너화 범위는 로컬 PC 의 물리 접근 조건에 달려 있다. 접근이 유지되면 eval 은 로컬에 남으므로 **학습 측만 컨테이너화하면 된다** (sim 을 컨테이너에 넣는 것은 Vulkan 때문에 별도 난이도다). 이 판단의 근거는 [remediation plan](../../../docs/superpowers/plans/2026-07-07-repo-review-remediation.md) 의 결정 #5 이고, 이번 주 진입 시 현재 상태로 다시 확인한다.


---


## 핵심 개념


### 1. 4070 으로 안 되는 이유는 옵티마이저가 아니다


흔한 오해부터 정정한다. LoRA 를 12GB 에서 못 돌리는 이유는 옵티마이저 상태나 활성값 때문이 아니다.


| 항목 | 크기 | 비고 |
|---|---|---|
| **frozen base 가중치 (bf16)** | **약 15GB** | 7B x 2바이트. **이것만으로 12GB 를 넘는다** |
| LoRA 어댑터 가중치 | 수십 MB | rank 32 기준 |
| 어댑터의 옵티마이저 상태 | 수백 MB | 학습 대상이 어댑터뿐이라 작다 |
| 활성값 | 배치에 비례 | batch 를 줄이면 줄어든다 |


즉 **바닥을 결정하는 것은 frozen base 다.** LoRA 의 이점은 "학습 대상을 줄여 옵티마이저·그래디언트를 줄이는 것" 이지 base 를 줄이는 것이 아니다. batch 를 1 로 줄여도 15GB 는 그대로 남는다.


upstream 이 제시하는 수치도 이 구조와 맞는다 — 배치 16 에서 약 72GB, 배치를 줄이면 **약 27GB 까지** 내려간다. 27GB 라는 하한이 존재하는 이유가 바로 base 가중치이고, 그래서 24GB 급 GPU 가 필요하다.


### 2. batch size 와 grad accumulation


메모리를 줄이면서 학습의 성질을 유지하는 표준 수단이다.


- **batch size** 를 줄이면 활성값이 줄어 메모리가 내려간다. 대신 한 스텝의 그래디언트가 노이즈해진다
- **grad accumulation** 은 여러 스텝의 그래디언트를 모아 한 번 갱신한다. 유효 배치를 유지하면서 메모리를 낮춘다
- 대가는 시간이다. 유효 배치를 유지하려면 순전파·역전파 횟수가 그만큼 늘어난다


따라서 24GB 에서는 `batch_size` 를 낮추고 `grad_accumulation_steps` 를 올려 유효 배치를 맞추는 조합을 찾는 것이 실습 3 의 일이다. **메모리는 batch 가, 시간은 유효 배치가 결정한다.**


### 3. 4-bit 학습 경로는 있지만 쓰지 않는다


upstream 학습 스크립트에는 4-bit 양자화 학습 옵션이 있다. 켜면 base 가 4바이트에서 0.5바이트급으로 내려가 12GB 안에 들어올 여지가 생긴다. 그런데 이번 Phase 에서는 쓰지 않는다.


| 이유 | 설명 |
|---|---|
| upstream 경고 | 스크립트 자체가 "메모리는 줄지만 성능을 해친다" 고 명시한다 |
| **교란 변인** | 산출물이 before/after 정량 분석이다. 학습 경로가 비표준이면 "성공률이 안 올랐다" 의 원인이 데이터인지 4-bit 학습인지 분리되지 않는다 |
| 비용 대비 | 24GB 클라우드 인스턴스 비용은 1사이클 기준 미미하다. 위 리스크를 감수해 아낄 금액이 아니다 |


이것이 "RunPod 을 쓰는 이유" 의 정확한 서술이다 — 로컬 PC 를 못 만져서가 아니라 **표준 경로로 돌려 변인을 줄이기 위해서**다. 물리 접근 조건이 바뀌어도 이 판단은 유지된다.


### 4. `max_steps` 기본값은 우리 예산의 규모가 아니다


학습 스크립트의 기본 스텝 수는 20만 스텝 규모다. 사전학습급 실험을 전제한 값이므로, 그대로 실행하면 **예산을 초과하기 전에 끝나지 않는다.**


우리는 반대 방향으로 정한다.


```mermaid
flowchart LR
    A["예산<br/>시간 · 비용"] --> B["probe 로 측정<br/>스텝당 초"]
    B --> C["가능한<br/>총 스텝 수"]
    C --> D["max_steps<br/>명시 지정"]
```


probe 로 스텝당 시간을 재고 거기서 역산한다 (실습 3). 이 값은 week1 §6 에서 데이터 규모를 정할 때 세운 "컴퓨트 예산" 방향과 같은 계산이며, 여기서 실측으로 채워진다.


### 5. 체크포인트는 선택이 아니다


Community Cloud 인스턴스는 회수될 수 있다. 학습이 몇 시간 단위면 회수 확률은 무시할 수 없다.


| 수단 | 역할 |
|---|---|
| network volume | pod 를 멈춰도 남는 저장 공간. 체크포인트를 여기에 둔다 |
| 주기 저장 (`save_steps`) | 회수 시점 이후의 진행만 잃는다 |
| 최신만 덮어쓰기 vs 시점별 보관 | 덮어쓰기는 용량이 작고, 시점별 보관은 되돌릴 수 있다 |


마지막 항목은 트레이드오프다. 학습이 발산했을 때 이전 체크포인트로 돌아가려면 시점별 보관이 필요하고, 용량이 부담이면 덮어쓰기를 쓴다. **어느 쪽을 골랐는지 기록**해야 실습 5 의 복구 리허설이 성립한다.


그리고 유휴 과금이 있다. 작업이 끝나면 pod 를 멈춘다 — volume 은 남는다.


### 6. 학습 통계가 함께 저장된다 — 추론의 `unnorm_key` 가 여기서 생긴다


학습 스크립트는 데이터셋 통계를 실행 디렉터리에 함께 저장한다. 이것이 week2 §4 의 오른쪽 갈래다.


```mermaid
flowchart LR
    A["week2 RLDS<br/>원시 단위 라벨"] --> B["학습 중<br/>통계로 정규화"]
    B --> C["통계가 실행 디렉터리에<br/>함께 저장"]
    C --> D["Section 3 추론<br/>unnorm_key = 내 데이터셋"]
```


**따라서 체크포인트만 회수하고 통계 파일을 빠뜨리면 fine-tuned 모델을 쓸 수 없다.** week0 에서 남의 로봇 통계를 빌려 썼던 자리에 이제 내 통계가 들어가야 하는데, 그 통계가 없으면 역정규화 기준이 사라진다. 회수 목록에 반드시 넣는다 (실습 4).


### 7. 체크포인트 저장 시 어댑터가 이미 머지된다


학습 스크립트는 체크포인트를 저장할 때 LoRA 어댑터를 base 에 머지해 저장한다. 즉 **week4 의 "머지" 작업 상당 부분이 여기서 끝난다.**


week4 에 남는 것은 머지된 가중치를 로컬로 내리고 **4-bit 양자화 + 호환성 검증**을 하는 일이다. 다만 어댑터 원본도 함께 보관하는 편이 낫다 — rank 를 바꿔 비교하거나 머지를 다시 하고 싶을 때 필요하다.


### 8. loss 가 내려가는 것은 통과 기준이 아니다


week2 §8 에서 본 조용한 실패들은 loss 를 정상적으로 내려가게 만든다. 라벨이 일관되게 틀려 있으면 모델은 그 틀린 라벨을 잘 맞추게 되고, loss 는 예쁘게 떨어진다.


이번 주의 통과 기준은 따라서 loss 곡선이 아니다.


| 통과 기준 | 확인 방법 |
|---|---|
| 1사이클 완주 | 지정한 스텝까지 도달하고 최종 체크포인트가 남았다 |
| 실측 기록 | 스텝당 시간, 총 시간, 비용, VRAM 피크 |
| 복구 가능 | 체크포인트에서 재개해 학습이 이어진다 (실습 5) |
| 회수 목록 완비 | 머지 가중치 + 어댑터 + **통계 파일** + 학습 설정 |


성공률이 올랐는지는 Section 3 의 eval 이 답한다. 이번 주가 답할 질문이 아니다.


---


## 자체 점검


문서를 보지 않고 답한다. 막히면 해당 절로 돌아간다.


**Q1.** LoRA 를 12GB 에서 못 돌리는 이유를 항목별 크기로 설명하라. 옵티마이저 상태가 주범이 아닌 이유는? (§1)


**Q2.** 27GB 라는 하한이 존재하는 이유는 무엇인가. batch 를 1 로 줄여도 내려가지 않는 이유는? (§1)


**Q3.** batch size 와 유효 배치가 각각 무엇을 결정하는가. (§2)


**Q4.** 4-bit 학습 경로를 쓰지 않는 이유 중 "비용" 이 아닌 것을 설명하라. (§3)


**Q5.** `max_steps` 를 정하는 방향이 왜 예산에서 시작하는가. (§4)


**Q6.** 체크포인트만 회수하고 통계 파일을 빠뜨리면 무엇이 불가능해지는가. (§6)


**Q7.** loss 가 매끄럽게 내려갔는데도 이번 주를 통과로 볼 수 없는 경우를 하나 들라. (§8)


---


## 이번 주 실습 & 다음 주 준비


### 이번 주 실습 과제

1. `Dockerfile` — 학습 측 이미지 (week2 명세 + 등록 패치 적용)
2. RunPod 기동·이관 기록 — `outputs/runpod_setup.md`
3. `practice_probe_run.sh` — 짧은 probe + 실측 -> `outputs/probe_measure.md`
4. `practice_train_cycle.sh` — 본 1사이클 + loss 추이 -> `outputs/train_log.md`
5. 중단 복구 리허설 -> `outputs/recovery_check.md`


### 이번 주 산출 (must / nice)

- must: 학습 측 이미지 + 실측치(스텝당 시간·총 시간·비용·VRAM 피크) + 머지 체크포인트 + 통계 파일 + 복구 리허설 통과
- nice: rank 변경 1회 비교 (예산이 남을 때만 — 본 사이클이 우선)


### 다음 주 (week 4) 준비

- week4 는 머지 가중치를 로컬로 내려 4-bit 양자화하고 호환성을 검증한다. 머지 자체는 이번 주 체크포인트 저장에서 이미 수행되므로(§7) week4 의 범위는 **전송 + 양자화 + 검증**이다
- 회수해야 할 것: 머지 가중치 / LoRA 어댑터 원본 / **통계 파일** / 학습 설정(스텝 수·배치·rank·학습률)
- 실측치는 `Measurements/` 에도 남긴다 — Section 0 의 "LoRA 1사이클 실측" 이 이 기록으로 닫힌다


---


## 이번 주 핵심 요약


1. **바닥은 frozen bf16 base 약 15GB** — 옵티마이저가 아니다. 그래서 24GB 급이 필요하다.
2. **batch 는 메모리를, 유효 배치는 시간을 결정한다** — grad accumulation 으로 둘을 분리한다.
3. **4-bit 학습은 안 쓴다** — 교란 변인을 만들지 않기 위해서다. 비용 문제가 아니다.
4. **`max_steps` 는 예산에서 역산한다** — 기본값은 우리 규모가 아니다.
5. **통계 파일도 회수 대상이다** — 없으면 fine-tuned 모델을 추론에 못 쓴다.
6. **loss 곡선은 통과 기준이 아니다** — 완주 · 실측 · 복구 · 회수 목록이 기준이다.


---


이전: [Week 2 — OpenVLA 학습 포맷으로 데이터 변환](../week2/README.md)


다음: [Week 4 — 로컬 머지 + 4-bit 양자화 + 호환성 검증](../week4/README.md)
