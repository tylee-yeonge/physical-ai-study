# Week 2 — OpenVLA 학습 포맷으로 데이터 변환


> **이번 주 목표**: week1 데이터셋을 RLDS 로 변환하고, OpenVLA 데이터로더에 등록해 배치가 실제로 꺼내지는 것까지 확인한다.
> **예상 시간**: 8-10시간
> **핵심 질문**: "학습 스크립트가 내 데이터를 읽었다는 것을, 학습을 돌리지 않고 어떻게 확인하는가?"
> **선행**: [week1](../week1/README.md) 완료 — `outputs/dataset/ep*.npz`, `collect_meta.json`, round-trip 검증 통과
> **대응 Roadmap**: [`Roadmap/Phase 4.5.md`](../../../Roadmap/Phase%204.5.md) Section 1 주차 2


---


## 이 문서를 읽는 법


- 이 문서는 **개념**(왜 이렇게 하는가), [`PRACTICE.md`](PRACTICE.md) 는 **절차**(무엇을 타이핑하고 무엇을 확인하는가) 다.
- 이번 주는 새 지식보다 **낯선 도구 이름**이 많다 (RLDS, TFDS, mixture, transform). 이름 자체가 어려운 것이 아니므로, 아래 용어 표에서 "이게 무슨 역할을 하는 물건인가" 만 잡고 진행한다.
- 자체 점검의 답은 문서에 적어 두지 않는다.


## 이번 주를 한 문장으로


> week1 이 만든 `.npz` 파일 더미를 **OpenVLA 학습 스크립트가 읽을 수 있는 형식**으로 바꾸고, "이런 데이터셋이 있다" 고 학습 코드 세 곳에 등록한 뒤, 학습을 돌리지 않고 **배치 하나만 꺼내 내용이 맞는지 검사**한다.


이번 주가 왜 한 주를 다 쓰는지 미리 짚는다. 데이터를 옮기는 작업은 단순하지만, **이번 주의 실패는 예외를 내지 않는다.** 라벨을 미리 정규화하거나 회전 표현을 잘못 저장해도 파일은 정상적으로 만들어지고, 다음 주 학습은 정상적으로 돌고 loss 도 예쁘게 내려간다. 그래서 "돌아간다" 를 통과 기준으로 쓸 수 없고, 대신 **배치를 꺼내 항목별로 검사하는 것**이 이번 주의 실질적 산출물이다 (§7-8).


---


## 학습 순서


| 순서 | 단계 | 파일/자료 | 설명 |
|:----:|------|----------|------|
| 1 | 포맷 요건 확정 | `PRACTICE.md` 1 | RLDS + 등록 3파일의 역할을 upstream 코드에서 확인 |
| 2 | RLDS 빌더 작성 | `PRACTICE.md` 2 | week1 npz -> RLDS 변환 |
| 3 | 데이터로더 등록 | `PRACTICE.md` 3 | configs / transforms / mixtures 3곳 |
| 4 | 로드 검증 | `PRACTICE.md` 4 | 배치 1개를 꺼내 스키마·범위 검사 |
| 5 | 정규화 계약 확인 | `PRACTICE.md` 5 | 통계 캐시 + gripper absolute 확인 |
| 6 | 퀴즈 | quiz_easy / quiz_medium | 스키마 / 정규화 / 등록 |


---


## 미리 풀어 두는 용어


| 용어 | 뜻 |
|---|---|
| **RLDS**(Reinforcement Learning Datasets) | 로봇·강화학습 데이터를 "episode 안에 step 들이 들어 있는" 구조로 저장하는 공통 형식 |
| **episode** | 시연 1회의 기록. step 들의 순서 있는 묶음 |
| **step** | 한 순간의 기록. 관측·action·경계 플래그가 붙는다 |
| **TFDS**(TensorFlow Datasets) | 데이터셋을 정의하고 빌드하는 라이브러리. RLDS 데이터는 보통 이 도구로 만든다 |
| **TFRecord** | TFDS 가 데이터를 디스크에 굽는 바이너리 파일 포맷 |
| **tf.data** | TensorFlow 의 데이터 스트리밍 파이프라인 API. TFDS 로 로드한 데이터셋이 이 형태로 나온다 |
| **shard** | 큰 데이터셋을 여러 파일 조각으로 나눈 것. 병렬로 읽기 위한 분할이다 |
| **빌더**(builder) | "내 원본 파일을 읽어 이런 모양의 데이터셋으로 만들어라" 를 적은 파이썬 클래스 |
| **feature 명세** | 각 항목의 모양(shape)과 자료형(dtype)을 미리 선언한 것. 선언과 실제 데이터가 다르면 빌드가 막힌다 |
| **`tfds build`** | 빌더 정의를 실제 데이터 파일로 굽는 명령 |
| **데이터로더**(dataloader) | 저장된 데이터셋에서 학습용 묶음(배치)을 꺼내 주는 코드 |
| **배치**(batch) | 한 번의 학습 갱신에 함께 넣는 샘플 묶음 |
| **스키마**(schema) | 데이터의 항목 이름·모양·자료형 구조 |
| **upstream** | 내가 고쳐 쓰는 원본 공개 리포지토리. 여기서는 openvla 리포 |
| **레지스트리**(registry) | "이름 -> 함수/설정" 을 적어 두는 딕셔너리. 이름이 없으면 로더가 그 데이터셋을 못 찾는다 |
| **mixture** | 학습에 쓸 데이터셋 조합과 그 비율. 데이터셋 1개여도 조합으로 지정해야 한다 |
| **transform 함수** | 내 원시 필드를 파이프라인이 기대하는 표준 필드로 옮기는 함수 |
| **action 인코딩** | action 벡터의 구성 규약. 예: "위치 delta 3 + RPY 3 + gripper 1 = 7" |
| **정규화 통계** | 데이터셋 전체의 action 분포 요약(분위수 등). 정규화·역정규화의 기준이 된다 |
| **정규화 마스크** | 차원별로 정규화를 적용할지 여부를 적은 True/False 배열 |
| **절대값 마스크** | 차원별로 "상대값인가 절대값인가" 를 적은 True/False 배열. 이름이 비슷하지만 정규화 마스크와 용도가 다르다 (§2.2) |
| **absolute** | 정규화를 거치지 않고 원래 값 그대로 통과하는 차원. gripper 가 그렇다 |
| **`unnorm_key`** | 추론 시 "어느 데이터셋 통계로 역정규화할지" 고르는 스위치 |
| **캐시**(cache) | 한 번 계산한 결과를 파일로 저장해 두고 다음에는 재사용하는 것 |
| **조용한 실패**(silent failure) | 예외도 경고도 없이 결과만 틀리는 실패. 이번 주 실패의 대부분이 이 형태다 |


---


## 시작하기 전에


변환은 TensorFlow 계열 도구를 쓴다. week0-1 의 sim venv 는 ManiSkill 과 torch 조합으로 묶여 있으므로, 여기에 TensorFlow 계열을 얹으면 의존성이 충돌해 앞의 환경이 깨질 수 있다. 그래서 **별도 venv 를 만든다.** 이 환경이 그대로 **Section 0 후반 Docker 이미지의 학습 측 명세**가 된다 — 여기서 겪는 설치 문제를 기록해 두면 그 작업이 짧아진다.


```bash
cd "/workspace/study/physical-ai-study/Studies/Phase 4.5"
python3 -m venv .venv-rlds                          # 변환·등록·로드 검증용
source .venv-rlds/bin/activate
pip install -r week2/requirements.txt
mkdir -p week2/outputs
git clone https://github.com/openvla/openvla ~/openvla         # 등록할 대상 코드
git clone https://github.com/kpertsch/rlds_dataset_builder ~/rlds_dataset_builder
```


두 리포를 받는 이유가 다르다. `openvla` 는 **내가 고쳐야 하는 대상**(등록 3파일이 그 안에 있다) 이고, `rlds_dataset_builder` 는 **베껴 쓰는 본보기**(빌더 템플릿) 다.


---


## 핵심 개념


### 1. RLDS 가 요구 사항인 이유


OpenVLA 의 파인튜닝 스크립트는 **RLDS** 형식의 데이터셋을 읽는다. RLDS 는 로봇 데이터를 "episode 하나 안에 step 여러 개가 순서대로 들어 있고, 각 step 에 관측·action·지시문이 붙어 있는" 구조로 저장하는 공통 형식이다. 사전학습과 논문의 파인튜닝 실험이 모두 이 경로로 돌았기 때문에, 검증된 길이 이것 하나다.


직접 로더를 쓰는 대안도 있다. week1 의 `.npz` 를 읽는 파이썬 코드를 짜서 학습 스크립트에 꽂는 것이 기술적으로 더 쉬워 보일 수 있다. 그런데 이번 Phase 에서는 택하지 않는다. 목적이 "adaptation 파이프라인을 설계-실행하고 정량 분석" 인데, 로더를 자작하면 나중에 결과가 안 나올 때 **원인 후보에 '내 로더' 가 추가**된다. week6 의 "배제된 후보" 표에 한 줄을 더 남겨 두는 셈이고, 그 한 줄을 배제하려면 또 검증을 해야 한다. 검증된 경로를 쓰고 변인을 줄이는 것이 싸다.


변환은 직접 만들지 않고 공개된 빌더 템플릿(`rlds_dataset_builder`)을 쓴다 — upstream README 가 그것을 안내한다.


그 RLDS 가 실제로 어떤 모양인지를 아래 세 절에서 편다 — 구조(1.1), 굽고 찾는 도구 TFDS(1.2), 읽기(1.3).


#### 1.1 구조 — episode 안에 step


이름에 Reinforcement Learning 이 들어가지만 강화학습 전용이 아니다. 구글 리서치가 2021년에 공개한 형식으로, 모방학습 시연처럼 **"시간 순서대로 진행되는 행동 기록"이면 전부** 담는다. Open X-Embodiment 가 수십 개 연구실의 로봇 데이터를 이 형식으로 통일했고, OpenVLA 는 그 데이터로 사전학습됐다 — 위에서 말한 "검증된 길" 이 여기서 나온다.


RLDS 데이터셋은 항상 아래 3단 계층이다.


```
dataset                          <- 데이터셋 전체 (예: week1 시연 모음)
+-- episode 0                    <- 시연 1회 (예: 집기 1번째 시도)
|   +-- episode_metadata         <- 에피소드 단위 정보 (원본 파일 경로 등)
|   +-- steps
|       +-- step 0               <- 한 순간의 기록
|       +-- step 1
|       +-- ...
+-- episode 1
+-- ...
```


step 은 "그 순간 로봇이 본 것 + 한 것" 의 묶음이다. 표준 필드는 다음과 같다.


| 필드 | 의미 | 우리 데이터에서 |
|---|---|---|
| `observation` | 그 순간의 관측. 중첩 dict 가능 | 카메라 이미지 + 관절 state |
| `action` | 그 관측을 보고 취한 행동 | 7차원 벡터 (§3 의 인코딩) |
| `reward` / `discount` | 보상 / 할인율 | 시연 데이터라 형식만 채운다 (0 과 1.0) |
| `is_first` | 에피소드의 첫 step 인가 | 경계 표시 |
| `is_last` | 에피소드의 마지막 step 인가 | 경계 표시 |
| `is_terminal` | 과제가 "진짜로 끝나서" 종료됐는가 | 아래 구분 참고 |
| `language_instruction` | 언어 지시문 | week0-1 과 같은 문구 (§7) |


`is_last` 와 `is_terminal` 의 구분이 처음에 가장 헷갈린다.


- `is_last=True, is_terminal=True`: 과제가 **진짜로 끝나서** 기록이 끝남
- `is_last=True, is_terminal=False`: 끝나지 않았는데 **기록이 잘림** (시간 제한, 녹화 중단)


성공한 시연만 저장하는 우리 데이터에서는 마지막 step 에 둘 다 True 를 주면 되지만, 이 구분을 알아야 빌더 템플릿 코드를 읽을 때 헷갈리지 않는다. 이 플래그들이 §7 검사 항목의 "episode 경계" 를 지탱한다.


`language_instruction` 은 원래 RLDS 표준에 없던 필드인데, Open X-Embodiment 계열 로봇 데이터셋이 관례로 추가해 사실상 필수가 됐다. VLA 모델이 이 지시문을 입력으로 쓰기 때문이다.


#### 1.2 TFDS — 데이터셋을 코드로 정의하고 이름으로 찾는다


**TFDS**(TensorFlow Datasets)는 구글이 만든 **데이터셋 관리 라이브러리**다. 원래는 MNIST, ImageNet 같은 공개 데이터셋을 "이름 하나로 내려받아 쓰게" 해 주는 도구로 유명하고, 그 관리 방식을 내 데이터에도 그대로 적용할 수 있다. 핵심 아이디어는 두 가지다.


1. **데이터셋을 코드로 정의한다.** "내 원본 파일을 읽어 이런 모양으로 구워라" 를 빌더 클래스 하나에 적는다. 데이터 준비가 사람마다 다른 수작업이 아니라 누가 실행해도 같은 결과가 나오는 코드가 된다.
2. **이름과 버전으로 찾는다.** 구운 데이터셋은 `이름/버전` 디렉터리에 들어가고, 읽는 쪽은 파일 경로 대신 이름으로 로드한다. §2 의 등록 3파일이 전부 "이름" 을 적는 것도 이 방식 위에 서 있기 때문이다.


이번 주 내내 비슷한 이름 네 개가 섞여 나오므로 역할을 갈라 둔다.


| 이름 | 역할 | 비유 |
|---|---|---|
| RLDS | 구조의 약속 — "episode 안에 step" | 규격서 |
| TFDS | 약속대로 정의하고 굽고 찾아 주는 도구 | 공장 + 카탈로그 |
| TFRecord | 디스크 위의 실제 파일 포맷 | 포장 상자 |
| tf.data | 로드된 데이터를 흘려보내는 파이프라인 | 컨베이어 벨트 |


**빌더가 하는 일.** 빌더 클래스에서 채울 곳은 두 군데뿐이다.


- `_info()`: feature 명세 선언. "step 에는 이미지가 256x256x3 uint8, action 이 float32 7차원" 처럼 모든 필드의 모양·자료형을 미리 적는다
- `_generate_examples()`: 원본 파일(week1 npz)을 읽어 episode dict 를 하나씩 yield 하는 함수


빌더 클래스 이름이 곧 데이터셋 이름이 된다 — `ManiskillPickcube` 클래스는 `maniskill_pickcube` 라는 이름으로 구워진다. 그리고 `_info()` 의 선언과 `_generate_examples()` 가 실제로 내놓는 데이터가 다르면 **빌드가 그 자리에서 막힌다.** 조용한 실패가 대부분인 이번 주에서 드물게 시끄러운 안전장치이므로, 명세를 대충 넓게 잡지 말고 정확히 적는 것이 이득이다.


**어디에 어떻게 구워지나.** 빌더 디렉터리에서 `tfds build` 를 치면 기본적으로 `~/tensorflow_datasets/` 아래에 결과가 떨어진다.


```
~/tensorflow_datasets/
+-- maniskill_pickcube/          <- 데이터셋 이름 (빌더 클래스 이름의 snake_case)
    +-- 1.0.0/                   <- 빌더의 VERSION
        +-- maniskill_pickcube-train.tfrecord-00000-of-00004    <- 데이터 본체 (shard)
        +-- ...
        +-- features.json        <- feature 명세 (어떤 필드가 어떤 모양·자료형인가)
        +-- dataset_info.json    <- 버전·split·episode 개수 등 메타데이터
```


- 데이터 본체는 **TFRecord** 파일이다. 이미지도 JPEG 로 인코딩되어 안에 들어간다. 파일이 여러 조각(shard)으로 나뉘는 것은 정상이다 — 병렬로 읽기 위한 분할이다.
- 빌더의 `VERSION` 을 올리면 새 버전 디렉터리에 새로 굽는다. 데이터를 고쳐 재생성할 때 옛 데이터·옛 통계와 섞이는 사고(§6)를 막는 장치가 이것이다.
- 구워진 폴더는 **자기완결적**이다. `features.json` 이 스키마를 담고 있어서, 읽는 쪽은 빌더 코드 없이 이 폴더만 있으면 된다. week3 에서 RunPod 로 데이터를 옮길 때 이 폴더를 통째로 올리면 되는 이유다.


즉 이번 주 실습 2 의 실체는 "npz 를 읽어 episode dict 를 yield 하는 클래스 하나 작성 + 빌드 명령 실행" 이다.


**PyTorch 프로젝트가 왜 TensorFlow 도구를 쓰나.** OpenVLA 의 모델과 학습 루프는 PyTorch 다. 그런데 데이터 쪽만 TFDS/tf.data 를 쓴다 — Open X-Embodiment 데이터 전체가 RLDS/TFDS 로 배포됐고, tf.data 의 스트리밍 파이프라인이 대용량 로봇 데이터를 다루는 검증된 경로였기 때문이다. 즉 **데이터는 TF 계열로 읽고, 텐서만 PyTorch 로 넘긴다.** 그래서 학습·검증 환경에는 TF 와 torch 가 한집에 살아야 하고, 그 조합이 기존 sim venv 를 깨뜨릴 수 있어 "시작하기 전에" 에서 별도 venv 를 만들었다.


#### 1.3 읽는 쪽 — steps 는 리스트가 아니다


```python
import tensorflow_datasets as tfds

# 빌드된 RLDS 데이터셋을 이름으로 로드 (~/tensorflow_datasets 에서 찾는다)
ds = tfds.load("maniskill_pickcube", split="train")

# episode 단위로 순회
for episode in ds:
    # steps 도 순회로만 접근한다 (인덱싱 불가)
    for step in episode["steps"]:
        image = step["observation"]["image"]
        action = step["action"]
```


`episode["steps"]` 는 리스트가 아니라 **중첩된 tf.data.Dataset** 이다. 에피소드가 수천 step 이어도 메모리에 한 번에 올리지 않고 스트리밍으로 읽기 위한 설계다. 처음 보면 "왜 인덱싱이 안 되지?" 하고 당황하는 지점이고, 실습 4 의 로드 검증 코드에서 실제로 마주친다.


### 2. 등록 3파일 — 무엇을 어디에 적는가


데이터를 RLDS 로 바꿔 놓는 것만으로는 학습 스크립트가 그것을 찾지 못한다. 학습 코드는 "이름으로 데이터셋을 찾는" 구조이므로, upstream 코드 3곳에 이름과 설정을 등록해야 한다.


| 파일 | 등록 대상 | 적는 내용 |
|---|---|---|
| `prismatic/vla/datasets/rlds/oxe/configs.py` | `OXE_DATASET_CONFIGS` | 관측·action 공간의 명세 (어느 카메라 키가 primary 인가, state·action 인코딩이 무엇인가) |
| `prismatic/vla/datasets/rlds/oxe/transforms.py` | `OXE_STANDARDIZATION_TRANSFORMS` | 내 원시 필드를 표준 형태로 옮기는 변환 함수 |
| `prismatic/vla/datasets/rlds/oxe/mixtures.py` | `OXE_NAMED_MIXTURES` | 학습에 쓸 데이터셋 조합의 이름 |


역할을 한 줄로 구분하면 — **configs 는 "무엇이 어디 있는가", transforms 는 "그것을 표준 형태로 어떻게 옮기는가", mixtures 는 "무엇을 학습에 쓸 것인가"** 다.


비유로 정리하면 configs 는 창고의 물품 목록표, transforms 는 물품을 표준 규격 상자에 옮겨 담는 작업 지시서, mixtures 는 이번 주문에 어느 창고를 쓸지 적은 주문서다.


세 파일을 따로 외우면 헷갈린다. 실제로는 **한 줄의 호출 사슬 위에 순서대로 놓인 세 개의 조회 지점**이고, 그 순서를 알면 각 파일이 왜 그 내용을 담는지가 따라온다. 2.1 에서 사슬을 먼저 보고 2.2-2.4 에서 파일별로 들어간 뒤, 2.5 에서 "빠뜨리면 어떻게 실패하는가" 로 닫는다.


#### 2.1 세 파일은 한 줄의 사슬 위에 있다


학습 스크립트가 데이터로더를 만들면 세 레지스트리가 아래 순서로 차례로 조회된다.


```mermaid
flowchart TD
    A["학습 스크립트<br/>조합 이름을 넘긴다"] --> M["mixtures.py<br/>OXE_NAMED_MIXTURES"]
    M -->|"(데이터셋 이름, 가중치)<br/>목록"| B["데이터셋마다<br/>1회 반복"]
    B --> D["configs.py<br/>OXE_DATASET_CONFIGS"]
    B --> E["transforms.py<br/>OXE_STANDARDIZATION_TRANSFORMS"]
    D --> F["설정 한 뭉치로 합쳐<br/>로더에 전달"]
    E --> F
```


사슬의 가운데에 `make_oxe_dataset_kwargs` 라는 함수 하나가 있다 (`oxe/materialize.py:21-76`). 이 함수가 하는 일은 세 가지다.


1. configs 의 dict 를 복사해 온다
2. 거기에 **정규화 관련 마스크 두 개를 계산해 덧붙인다** (2.2)
3. transforms 의 함수를 `standardize_fn` 이라는 이름의 값으로 꽂아 넣는다


결과는 dict 하나다. 즉 **configs 와 transforms 는 최종적으로 한 덩어리로 합쳐져 로더에 들어간다.** 별개의 두 설정이 아니라 "명세 + 그 명세대로 옮기는 함수" 한 쌍이다. mixtures 는 그 앞에서 "이 작업을 어느 이름들에 대해 반복할지" 만 정한다.


#### 2.2 configs.py — 삭제되는 키가 가장 중요하다


등록하는 것은 dict 하나이고 키는 5개뿐이다. 그런데 파인튜닝 경로에서 **로더까지 살아서 도달하는 키는 5개 중 1개뿐이고, 나머지 4개는 중간에 삭제된다.** 그런데도 삭제되는 쪽에 이 파일에서 가장 중요한 키가 들어 있다. 이 비대칭이 configs.py 를 헷갈리게 만드는 지점이므로 먼저 갈라 둔다.


| 키 | 로더까지 가는가 | 무슨 일을 하나 |
|---|---|---|
| `image_obs_keys` | 간다 (primary 슬롯만 남고 나머지는 걸러진다) | 관측 필드에 슬롯 이름 붙이기 |
| `depth_obs_keys` | 삭제 — 파인튜닝 경로가 깊이를 안 읽는다 | 없음 |
| `state_obs_keys` | 삭제 — 파인튜닝 경로가 proprio 를 안 읽는다 | 없음 |
| `state_encoding` | 삭제 | 없음. 문서용 표식이다 |
| `action_encoding` | 삭제 | **삭제되기 직전에 마스크 두 개를 낳는다** |


**`image_obs_keys` 는 슬롯에 꽂는 일이다.** `{"primary": "image"}` 는 "내 데이터의 `image` 필드를 primary 슬롯에 꽂아라" 는 뜻이다. 방향이 헷갈리기 쉬운데 `{새 이름: 옛 이름}` 이고, 로더는 이것을 받아 `observation["image_primary"]` 를 만든다. 값에 `None` 을 적으면 필드가 사라지는 것이 아니라 **빈 패딩이 채워진다** — 카메라 개수가 다른 여러 데이터셋을 섞어도 배치 모양이 같아야 하기 때문이다.


**`action_encoding` 은 사라지기 전에 두 개의 마스크를 만든다.** 이것이 이 파일의 실질이다. EEF 위치 인코딩으로 적으면 파이프라인이 다음 두 배열을 자동으로 만들어 설정에 넣는다.


| 파생물 | 값 | 용도 |
|---|---|---|
| **정규화 마스크**(action_normalization_mask) | `[True]*6 + [False]` | 앞 6차원만 정규화. 7번째(gripper)는 원값 통과 (§5) |
| **절대값 마스크**(absolute_action_mask) | `[False]*6 + [True]` | 차원별로 "상대값인가 절대값인가" 표시 |


정규화 마스크가 실제로 쓰이는 자리는 정규화 함수의 조건 분기 한 줄이다 — 마스크가 True 인 차원만 `[-1, 1]` 로 옮기고, False 인 차원은 입력값을 그대로 내보낸다. §5 의 "gripper 는 absolute" 라는 계약이 코드에서는 그 한 줄이다.


절대값 마스크는 정규화와 무관하다. 이름이 비슷해 헷갈리는데 용도가 다르다 — 여러 스텝의 action 을 한 번에 예측할 때 에피소드 끝을 넘어가는 자리를 무엇으로 채울지에 쓰인다. 상대값 차원은 0 으로, 절대값 차원은 마지막 값을 반복해 채운다. 우리는 한 번에 한 스텝만 예측하므로 이 마스크는 실제로는 아무 일도 하지 않는다.


그런데 이 "아무 일도 안 하는" 마스크가 **이번 주에서 가장 쓸모 있는 안전장치**다. 로더가 마스크를 붙이기 전에 길이를 검사하기 때문이다 (`rlds/dataset.py:189-194`).


```python
# 마스크 길이와 실제 action 차원이 다르면 여기서 멈춘다
if len(absolute_action_mask) != traj["action"].shape[-1]:
    raise ValueError(...)
```


EEF 위치 인코딩(7차원)으로 등록해 놓고 빌더가 8차원 action 을 내놓으면 여기서 예외가 난다. **action 차원 불일치를 잡아 주는 유일한 자리**다. 실습 4 에서 이 마스크 값을 눈으로 확인하는 이유가 여기 있다 — 그 값이 찍혔다는 것은 "등록한 인코딩과 실제 데이터의 차원이 일치한다" 는 증거이기도 하다.


#### 2.3 transforms.py — 가장 먼저 호출되고, 에피소드를 통째로 받는다


**언제 불리나.** 로더가 파일에서 궤적을 읽어 낸 직후, 다른 어떤 처리보다 먼저다. 그래서 configs 의 `image_obs_keys` 에 적는 "옛 이름" 은 빌더가 저장한 이름이 아니라 **내 변환 함수가 반환한 뒤의 이름**이다. 두 파일이 이 지점에서 맞물린다. 변환 함수에서 이름을 바꾸지 않으면 결과적으로 같은 이름이지만, 순서를 알아 두어야 나중에 필드 이름을 바꿀 때 어디를 고칠지 헷갈리지 않는다.


**무엇을 받나.** 인자 이름이 `trajectory` 인 데서 짐작되듯 step 하나가 아니라 **에피소드 하나가 통째로** 들어온다. 모든 필드에 선두 시간 축이 붙어 있어서 action 은 `(7,)` 이 아니라 `(T, 7)`, 이미지는 `(T, 224, 224, 3)` 이다. 문서화가 잘 안 된 함정이고, 남의 변환 함수가 전부 `[:, :6]`, `[1:]`, `axis=-1` 같은 축 연산으로 쓰여 있는 이유가 이것이다.


**무엇을 하나.** 이미 등록된 데이터셋들의 변환 함수를 읽어 보면 하는 일이 네 종류로 갈린다.


| 하는 일 | 예 |
|---|---|
| 흩어진 필드를 하나의 action 벡터로 조립 | 위치 델타 + 회전 델타 + gripper 를 이어 붙여 7차원으로 |
| gripper 규약 맞추기 | 연속값을 0/1 로 이진화, 부호 뒤집기, 상대값을 절대값으로 |
| 지시문 필드 이름 통일 | `natural_language_instruction` -> `language_instruction` |
| 잘못된 step 제거 | 첫 step 의 action 이 전부 0 인 데이터셋에서 그 step 잘라내기 |


네 가지 모두 **"이미 굳어 있는 남의 원본" 을 표준에 맞추는 일**이다. 우리는 사정이 다르다. 원본을 직접 만들었으므로 이 네 가지를 week1-2 의 빌더 단계에서 이미 맞춰 저장했다. 그래서 **우리 변환 함수는 사실상 통과**가 된다. 무언가 빠뜨린 것이 아니라 같은 일을 앞당겨 한 결과다.


**하는 일이 없어도 등록은 반드시 해야 한다.** 로더는 레지스트리를 맨손으로 인덱싱하므로(`OXE_STANDARDIZATION_TRANSFORMS[dataset_name]`, `oxe/materialize.py:70`), 이름이 없으면 `KeyError` 로 막힌다. 반환값에는 `observation`, `action`, `language_instruction` 세 키가 반드시 있어야 하고, 없으면 로더가 "변환 함수를 작성했는가" 를 묻는 예외를 낸다.


**이 함수의 소스 코드가 통계 캐시 이름에 들어간다.** §6 과 직접 이어지는 사실이라 여기서 짚어 둔다 — 캐시 파일명은 (데이터셋 정보 + state 키 목록 + **이 함수의 소스 코드 문자열**) 을 해시한 값이다. 그래서 변환 함수를 손대면 공백 하나만 고쳐도 통계가 자동으로 재계산된다. 뒤집으면, 이 목록에 없는 것을 고쳤을 때는 재계산되지 않는다는 뜻이기도 하다 (§6).


#### 2.4 mixtures.py — 세 곳 중 유일하게 없어도 되는 곳


학습 스크립트는 조합 이름을 인자로 받는다. 그런데 로더에는 예비 경로가 있다 — 받은 이름이 `OXE_NAMED_MIXTURES` 에 없으면 **그 이름을 데이터셋 이름으로 간주하고 "그 데이터셋 하나짜리 조합" 을 즉석에서 만든다** (`vla/datasets/datasets.py:84-89`). 즉 mixtures.py 등록을 건너뛰고 데이터셋 이름을 직접 넘겨도 돌아간다.


그래도 등록하는 실익이 두 가지 있다. 조합을 이름 하나로 고정해 재현할 수 있다는 것, 그리고 나중에 데이터셋을 추가할 때 학습 명령을 바꾸지 않아도 된다는 것이다. 다만 **"빠지면 실패한다" 는 사실이 아니다** — 세 곳 중 configs 와 transforms 두 곳만이 없으면 즉시 막힌다.


가중치의 의미도 짚어 둔다. 목록에 적는 `1.0` 은 그대로 쓰이지 않고 **데이터셋의 프레임 수를 곱한 뒤 정규화된다.** 큰 데이터셋과 작은 데이터셋에 똑같이 1.0 을 주면 "한 바퀴 돌 때 각 데이터셋을 한 바퀴씩" 이 되도록 보정한다는 뜻이다. 단일 데이터셋에서는 어떤 값을 적어도 결과가 같다.


#### 2.5 빠뜨렸을 때 어떻게 실패하는가


실패 형태를 미리 갈라 두면 §7 로드 검증에서 무엇을 봐야 하는지가 분명해진다. **이번 주의 주제가 조용한 실패이므로, 어느 것이 시끄럽고 어느 것이 조용한지가 실질적인 정보다.**


| 빠뜨리거나 틀린 것 | 실패 형태 | 시끄러움 |
|---|---|---|
| configs 항목 자체 | `KeyError` | 즉시 막힌다 |
| transforms 레지스트리 등록 | `KeyError` | 즉시 막힌다 |
| `image_obs_keys` 의 키 이름 오타 | `KeyError` | 즉시 막힌다 |
| action 차원이 등록한 인코딩과 불일치 | `ValueError` (2.2 의 길이 검사) | 즉시 막힌다 |
| mixtures 항목 | 실패하지 않는다 (2.4 의 예비 경로) | 해당 없음 |
| 지원 목록 밖 `action_encoding` | 예외가 잡혀 경고로 강등되고 그 데이터셋이 조합에서 빠진다. 빈 조합으로 진행하다 엉뚱한 자리에서 터진다 | **로그를 읽어야 보인다** |
| `action_encoding` 을 다른 지원 인코딩으로 잘못 적음 | 마스크가 엉뚱하게 파생되어 gripper 가 정규화된다 | **조용하다** |


위쪽 네 줄은 검증하지 않아도 알아서 터진다. **아래 두 줄이 §7 로드 검증이 존재하는 이유다.**


### 3. action 인코딩이 회전 표현을 이미 결정해 놓았다


`configs.py` 에 적는 action 인코딩에는 정해진 종류가 있고, 각각이 action 벡터의 의미를 확정한다.


| 인코딩 | 벡터 구성 |
|---|---|
| EEF 위치 | EEF delta XYZ (3) + **RPY (3)** + gripper (1) = 7 |
| 관절 위치 | 관절 delta (7) + gripper (1) = 8 |
| EEF R6 | EEF delta XYZ (3) + R6 회전 표현 (6) + gripper (1) = 10 |


우리가 쓰는 것은 첫 줄(7차원)이다. 즉 **회전은 오일러각(RPY)으로 고정**이다 — week0 계약 표의 "회전 표현" 행에서 열어 두었던 선택지가 여기서 닫힌다. 축-각으로 라벨을 만들었다면 지금 고쳐야 한다.


"지금 고쳐야 한다" 가 뜻하는 비용을 분명히 해 둔다. 라벨을 고치려면 week1 실습 2 로 돌아가 회전 델타 계산을 바꾸고, 라벨을 전량 재생성하고, round-trip 검증을 다시 통과시키고, 이번 주 빌드를 다시 해야 한다. 그리고 통계 캐시도 버려야 한다 (§6). **뒤로 갈수록 이 사슬이 길어지므로 여기서 확인하는 것이 가장 싸다.**


이것이 week0 -> week1 -> week2 를 관통하는 계약의 마지막 조각이다. 세 주차가 같은 표를 채워 왔다.


### 4. 정규화는 파이프라인이 한다 — 저장은 원시 단위


학습 파이프라인은 데이터셋 전체를 한 번 훑어 **action 통계를 자동으로 계산하고 파일로 캐시한 뒤, 학습 중 그 통계로 action 을 정규화**한다.


왜 정규화를 하는가부터. 위치 델타는 미터 단위라 0.01 대의 값이고, 회전 델타는 라디안 단위라 0.1 대일 수 있다. loss 는 예측과 정답의 차이를 차원 구분 없이 합치므로, 값이 10배 큰 회전 차원의 오차가 loss 를 지배하고 위치 차원은 상대적으로 무시된다. 그래서 각 차원을 자기 분포 기준으로 `[-1, 1]` 대역에 맞춰 균형을 맞춘다.


비유로 잡으면 — 만점이 과목마다 다른 시험 점수를 공통 척도로 환산하는 것과 같다. 환산하려면 각 과목의 점수 분포를 알아야 하고(통계), 분포 조사를 매번 하면 낭비니 환산표를 한 번 만들어 두고(캐시), 성적 처리 때마다 그 표로 바꾼다(학습 중 정규화). 원본 성적표 자체는 고치지 않는다(원시 단위 저장).


이 절 첫 문장을 네 토막으로 자르면 각각 이런 뜻이다.


| 토막 | 뜻 |
|---|---|
| "데이터셋 전체를 한 번 훑어" | 학습 시작 전에 모든 episode 의 모든 step 에서 action 을 꺼내 차원별 값 범위를 조사한다. 손으로 할 일은 없다 |
| "통계를 자동으로 계산하고" | 조사 결과는 차원별 하한·상한 몇 개다. 최솟값/최댓값 대신 **1%/99% 분위수**를 쓴다 — 튀는 값 하나에 범위가 망가지지 않게 하기 위해서다 |
| "파일로 캐시한 뒤" | 훑기는 오래 걸리고 결과는 작은 json 하나다. 다음 실행부터는 재계산 없이 파일을 읽는다 — §6 함정의 뿌리가 여기다 |
| "학습 중 그 통계로 정규화한다" | 배치를 꺼내는 순간 메모리에서 변환해 모델에 준다. 디스크의 데이터는 끝까지 원시 단위 그대로다 |


변환 자체는 "자기 범위 안에서 어디쯤인가" 를 `[-1, 1]` 로 옮기는 계산이다: `(값 - 하한) / (상한 - 하한) * 2 - 1`. 숫자로 한 번 따라가면:


| | 위치 x 델타 | 회전 roll 델타 |
|---|---|---|
| 통계가 말하는 범위 | 하한 -0.02, 상한 +0.02 (m) | 하한 -0.2, 상한 +0.2 (rad) |
| 어떤 step 의 라벨 | 0.01 | 0.1 |
| 정규화 후 | 0.5 | 0.5 |


원래 10배 차이 나던 두 값이 둘 다 0.5 가 됐다 — "자기 분포 안에서 4분의 3 지점" 이라는 같은 의미이기 때문이다. 이제 두 차원이 loss 에 공평하게 기여한다. 단 gripper 차원은 이 변환에서 제외된다 (§5).


따라서 라벨은 **원시 물리 단위(미터·라디안)로 저장해야 한다.** 미리 `[-1, 1]` 로 맞춰 저장하면 정규화가 두 번 걸려 라벨이 망가진다. 이 오류는 예외를 내지 않는다 — 학습은 정상적으로 돌고 loss 도 내려간다. 결과만 조용히 틀린다.


```mermaid
flowchart LR
    A["week1 라벨<br/>원시 물리 단위"] --> B["파이프라인이<br/>통계 계산"]
    B --> C["통계 캐시<br/>json"]
    C --> D["학습 중<br/>자동 정규화"]
    C --> E["추론 시<br/>unnorm_key 로 역정규화"]
```


오른쪽 갈래가 Section 2-3 으로 이어진다. 정규화된 값으로 배운 모델은 정규화된 값을 뱉으므로, 로봇에 보내려면 같은 통계로 미터·라디안으로 되돌려야 한다(역정규화). 그래서 학습에 쓰인 통계가 모델과 함께 저장되고, 추론 때 `unnorm_key` 로 그 통계를 지정해 역정규화한다. 즉 **fine-tuned 모델의 `unnorm_key` 는 내 데이터셋 이름**이 된다. week0 에서 남의 로봇 통계(`bridge_orig`)를 빌려 쓴 것이 여기서 자기 통계로 바뀐다.


이 갈래를 지금 이해해 두면 week3-4 가 쉬워진다. 학습이 끝났을 때 회수해야 하는 것이 가중치만이 아니라 **이 통계 파일까지**라는 사실이 여기서 나온다 (week3 §6).


### 5. gripper 는 absolute 다 — 그 보장은 등록에서 나온다


파이프라인에는 명시된 계약이 있다. **EEF 위치 인코딩에서는 마지막 차원(gripper)만 absolute** 다. 즉 앞 6차원만 정규화되고 gripper 는 원값으로 통과한다.


왜 gripper 만 다른가. 앞 6차원은 "몇 미터 / 몇 라디안" 이라는 연속량이라 분포 기준으로 스케일을 맞추는 것이 자연스럽다. 반면 gripper 는 사실상 열림/닫힘 두 상태를 가리키는 신호다. 이것을 분포 기준으로 늘리고 줄이면 "반쯤 닫힘" 같은 무의미한 중간값이 만들어진다. 그래서 손대지 않고 통과시킨다.


중요한 것은 이 마스크를 손으로 넣지 않는다는 점이다. **`configs.py` 에 적은 action 인코딩에서 자동으로 파생된다** — EEF 위치 인코딩으로 등록하면 "앞 6개만 정규화" 마스크가 따라온다 (§2.2).


지원 목록 밖 인코딩(관절 위치 등)을 적으면 파이프라인이 그 데이터셋을 조합에서 빼 버린다. 예외가 발생하긴 하지만 상위 함수가 그것을 잡아 경고 로그로 강등하므로 **실행이 그 자리에서 멈추지 않는다.** 로그를 읽지 않으면 "데이터셋이 하나도 없는 조합으로 학습을 시작" 하는 상태를 놓친다 (§2.5).


따라서 gripper 보호의 실질적 장치는 "마스크를 잊지 않는 것" 이 아니라 **인코딩을 정확히 등록하는 것**이다. week0 실습 4 에서 사전학습 통계의 차원별 마스크를 확인한 것이 이 지점의 예습이었다.


여기서 따라오는 함의가 하나 있고, 그게 더 중요하다. gripper 는 정규화를 거치지 않으므로 **week1 에서 저장한 값이 그대로 학습된다.** 부호 규약이 틀렸다면 정규화가 그것을 보정해 주지 않는다 — week1 계약 표 5번 행이 여기서 되돌릴 수 없게 굳는다. 부호가 뒤집힌 채 학습하면 모델은 "집어야 할 때 펴는" 동작을 정확하게 배운다.


### 6. 통계 캐시는 낡을 수 있다


통계는 계산 후 파일로 캐시된다. 캐시가 있으면 다시 계산하지 않는다 — 즉 **데이터를 바꿨는데 캐시가 남아 있으면 옛 통계로 정규화된다.**


왜 데이터가 바뀐 것을 알아서 감지하지 못하는가. 캐시 파일의 이름이 **세 가지를 이어 붙여 해시한 값**이기 때문이다 (§2.3).


| 해시에 들어가는 것 | 무엇이 달라지면 바뀌나 |
|---|---|
| 데이터셋 정보 | 이름, 빌더 `VERSION`, split 별 episode 수 등 |
| `state_obs_keys` | configs 의 proprio 키 목록 |
| 변환 함수의 소스 코드 문자열 | transforms 의 내 함수 본문 |


여기서 읽어야 할 것은 목록에 **없는 것**이다. **action 값 자체는 해시에 들어가지 않는다.** 그래서 라벨 값만 고치고 episode 개수와 빌더 `VERSION` 을 그대로 둔 채 재빌드하면, 이름이 그대로여서 옛 통계 파일을 다시 집어 온다. 반대로 변환 함수를 손대면 공백 하나만 고쳐도 이름이 달라져 자동으로 재계산된다 — 위험한 쪽은 **파일 이름이 안 바뀌는 변경**이다.


대응은 둘 중 하나다. 데이터를 고칠 때 빌더 `VERSION` 을 올리거나(§1.2), 재빌드 후 통계 파일을 직접 지우는 것이다. 무엇을 하든 **재계산이 실제로 일어났는지 확인**하는 것까지가 절차다.


이 함정은 라벨을 고친 직후에 정확히 발생한다 (예: 회전 표현을 RPY 로 고쳐 재생성한 다음). 즉 **문제를 하나 고친 직후가 가장 위험한 순간**이다 — 방금 고쳤으니 맞을 것이라고 생각하게 되기 때문이다.


### 7. 로드 검증이 학습 전 마지막 게이트


학습을 돌리기 전에 **배치 하나를 실제로 꺼내 본다.** 여기서 잡히는 것과 학습 3시간 뒤에 잡히는 것의 비용 차이가 크다. 더 정확히는, 학습 3시간 뒤에는 **잡히지도 않는다** — loss 가 내려갔으므로 정상으로 보인다. 발견은 week5 의 eval 에서, 원인 추적은 week6 에서 하게 된다.


검사 항목:


| 항목 | 기대 |
|---|---|
| 이미지 텐서 | 형상과 dtype 이 모델 입력 규격과 맞는가 |
| action 차원 | 7 인가 |
| action 범위 (정규화 후) | 대략 `[-1, 1]` 대역인가. 자릿수가 다르면 이중 정규화 또는 통계 오류 |
| gripper 차원 | 정규화를 거치지 않고 원값 두 갈래로 남아 있는가 |
| instruction 문자열 | week0-1 과 **같은 문구**인가 (변하면 변인이 늘어난다) |
| episode 경계 | 스텝이 다른 episode 와 섞이지 않는가 |


세 번째 줄이 이중 정규화를 잡는 방식을 풀어 두면 이렇다. 정규화가 한 번만 걸렸으면 값이 `[-1, 1]` 근처에 퍼진다. 두 번 걸렸으면 이미 작아진 값을 또 나누므로 `[-0.05, 0.05]` 처럼 비정상적으로 좁은 대역이 된다. 즉 **범위의 자릿수 하나로 이중 정규화를 판정할 수 있다.**


### 8. "포맷팅이 adaptation 의 절반" 의 실제 의미


이 말은 작업량이 절반이라는 뜻이 아니다. **실패의 절반이 여기서 나오고, 그 실패가 조용하다**는 뜻이다.


이번 주에 조용히 틀릴 수 있는 것들:


- 라벨을 미리 정규화 (§4)
- 회전 표현 불일치 (§3)
- gripper 부호가 틀린 채 저장 (§5) — 정규화가 보정해 주지 않는다
- 낡은 통계 캐시 (§6)
- 라벨 인덱스가 한 스텝 밀림 (week1 실습 2)


다섯 개 모두 예외를 내지 않고 loss 도 내려간다. 그래서 §7 의 로드 검증이 이번 주의 실질적 산출물이다. 그리고 이 검사 로그가 week6 에서 "데이터가 학습에 안 들어갔다" 와 "라벨 이중 정규화" 두 후보를 배제하는 근거로 인용된다.


---


## 자체 점검


문서를 보지 않고 답한다. 막히면 해당 절로 돌아간다.


**Q1.** 데이터로더를 자작하지 않고 검증된 경로를 쓰는 이유를 Phase 4.5 의 성공 기준과 연결해 설명하라. (§1)


**Q2.** 등록 3파일의 역할을 각각 한 줄로 구분하라. 그중 등록하지 않아도 로더가 돌아가는 곳은 어디이며, 그런데도 등록하는 이유는 무엇인가. (§2, §2.4)


**Q3.** 회전 표현이 RPY 로 확정되는 근거는 어디에 있는가. (§3)


**Q4.** 라벨을 미리 `[-1, 1]` 로 정규화해 저장하면 어떤 증상이 나타나는가. 왜 발견하기 어려운가. (§4)


**Q5.** gripper 차원은 정규화에서 어떻게 다뤄지며 그 보장은 어디서 나오는가. 그 때문에 week1 의 무엇이 되돌릴 수 없게 굳는가. 그리고 action 인코딩에서 파생되는 마스크는 둘인데, 정규화를 담당하지 않는 다른 하나는 무슨 일을 하는가. (§5, §2.2)


**Q6.** 라벨을 고쳐 데이터를 재생성한 직후에 특히 주의할 것은 무엇인가. 통계 캐시가 낡은 채로 남는 일이 왜 자동으로 감지되지 않는지를, 캐시 파일 이름이 정해지는 방식과 연결해 설명하라. (§6)


**Q7.** 학습을 돌리지 않고 데이터 연결을 확인하는 방법과, 그때 검사할 항목 4개 이상. (§7)


---


## 이번 주 실습 & 다음 주 준비


### 이번 주 실습 과제

1. 포맷 요건 확정 — `outputs/format_spec.md` (등록 3파일의 역할 + action 인코딩 확정)
2. `practice_build_rlds.py` — week1 npz -> RLDS 변환 (빌더 템플릿 사용)
3. 등록 3파일 수정 — 변경 내용을 `outputs/registration.md` 에 diff 로 기록
4. `practice_load_check.py` — 배치 1개 꺼내 7항목 검사
5. `practice_norm_check.py` — 통계 json 확인 + gripper 마스크 검증


### 이번 주 산출 (must / nice)

- must: RLDS 데이터셋 1벌 + 등록 3파일 수정 기록 + 로드 검증 통과 로그
- nice: 변환 중 겪은 조용한 실패 사례 메모 (블로그 소재 — §8 의 5개 중 실제로 밟은 것)


### 다음 주 (week 3) 준비

- week3 은 RunPod 에서 LoRA 를 돌린다. 이번 주의 `.venv-rlds` 구성이 그 환경의 원본이다
- week3 진입 전에 Section 0 후반 (Docker 컨테이너화 + RunPod 이관 + LoRA 1사이클 실측) 이 선행 조건이다 — week3 자료에서 다룬다
- 등록 3파일 수정은 upstream 리포에 대한 로컬 변경이다. 즉 **내 컴퓨터의 리포에만 있는 변경**이므로, 클라우드에서 같은 코드를 띄우려면 그 변경을 재현할 방법이 필요하다. 어떻게 재현할지(패치 파일 / fork) 를 week3 컨테이너화에서 결정해야 하므로 `outputs/registration.md` 를 정확히 남긴다


---


## 이번 주 핵심 요약


1. **RLDS + 등록 3파일** — 변환만으로는 학습 스크립트가 데이터를 못 찾는다.
2. **action 인코딩이 회전 표현을 확정한다** — 7차원 = XYZ delta + RPY + gripper.
3. **정규화는 파이프라인 몫** — 라벨은 원시 물리 단위로 저장한다. 미리 정규화하면 이중 정규화다.
4. **gripper 는 absolute** — 인코딩 등록에서 마스크가 파생된다. 정규화가 부호 오류를 보정해 주지 않는다.
5. **통계 캐시를 의심한다** — 데이터를 고쳤으면 캐시도 버린다.
6. **로드 검증이 이번 주의 산출물이다** — 이번 주의 실패는 전부 조용하다.


---


이전: [Week 1 — sim task 정의 + adaptation 데이터 수집](../week1/README.md)


다음: [Week 3 — RunPod LoRA 파인튜닝](../week3/README.md)
